using UnityEngine;
/// <summary>
/// 편지 받음/읽음 상태 저장
/// </summary>
public class MailboxSaveData : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

using UnityEngine;
/// <summary>
/// 버튼 눌러서 여는 화면 + 본문 표시까지 포함
/// </summary>
public class MailButton : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
